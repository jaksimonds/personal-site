import Hero from '@/components/Hero'

const NotFound = () => (
  <div>
    <Hero
      heading='404'
      intro={'The page you have requested isn\'t currently supported. It\'s possible the original page was removed or a link has not been updated.'}
    />
  </div>
)

export default NotFound